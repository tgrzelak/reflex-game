import { ConvertDifficultyPipe } from './convert-difficulty.pipe';

describe('ConvertDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
